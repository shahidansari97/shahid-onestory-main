import React from 'react';
import Button from "@/Components/UI/Button.jsx";

export default function WithdrawalSuccessModal({closeModal}) {
    return (
        <>
            <div className="os-title os-title--h4 os-title--bold">You have successfully made the withdrawal. The money will arrive within a few hours.</div>
        </>
    );
}
